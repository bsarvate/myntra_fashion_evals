# Copy to .env and fill in local values. Both notebooks load it with python-dotenv.
# Existing environment variables take precedence. Colab Secrets also remain supported.
NEBIUS_API_KEY=
NEBIUS_MODEL=
PINECONE_API_KEY=
PINECONE_TEXT_INDEX=fashion-text
PINECONE_IMAGE_INDEX=fashion-images
